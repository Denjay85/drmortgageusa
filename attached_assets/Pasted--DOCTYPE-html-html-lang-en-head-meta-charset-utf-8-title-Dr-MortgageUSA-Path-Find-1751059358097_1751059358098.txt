<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Dr.MortgageUSA – Path Finder</title>
  <!-- Tailwind via CDN for quick prototyping. Replace with your build pipeline in production -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-900 font-sans">
  <!-- ── HERO ─────────────────────────────────────────────────────────────── -->
  <section id="hero" class="text-center py-16 px-4">
    <h1 class="text-3xl md:text-4xl font-bold mb-6 max-w-4xl mx-auto">
      Unlock Your Path to Homeownership—<span class="text-blue-600">No Guesswork, No Gimmicks.</span>
    </h1>
    <button id="startQuiz" class="bg-blue-600 hover:bg-blue-700 transition text-white px-8 py-4 rounded-2xl shadow-lg">
      Start My 60‑Second Path&nbsp;Finder
    </button>
  </section>

  <!-- ── PATH‑FINDER QUIZ (modal) ─────────────────────────────────────────── -->
  <div id="quizModal" class="fixed inset-0 bg-black/50 flex items-center justify-center hidden z-50">
    <div id="quizCard" class="bg-white rounded-2xl shadow-2xl p-8 w-full max-w-md relative">
      <!-- Close on outside click handled in JS -->
      
      <!-- Step 1 ----------------------------------------------------------------- -->
      <div data-step="1" class="quiz-step">
        <h2 class="text-xl font-semibold mb-4">What's your first name?</h2>
        <input id="firstName" type="text" placeholder="First name" class="w-full border rounded-md p-2 mb-6"/>
        <button class="nextBtn bg-blue-600 hover:bg-blue-700 transition text-white px-4 py-2 rounded-md w-full">Continue</button>
      </div>

      <!-- Step 2 ----------------------------------------------------------------- -->
      <div data-step="2" class="quiz-step hidden">
        <h2 class="text-xl font-semibold mb-4">Which describes you best?</h2>
        <div class="grid gap-3">
          <button class="segmentBtn bg-gray-100 hover:bg-gray-200 px-4 py-2 rounded-md" data-segment="firstTimer">First‑Time Buyer</button>
          <button class="segmentBtn bg-gray-100 hover:bg-gray-200 px-4 py-2 rounded-md" data-segment="veteran">Veteran / Active Duty</button>
          <button class="segmentBtn bg-gray-100 hover:bg-gray-200 px-4 py-2 rounded-md" data-segment="creditBoost">Credit‑Rebuild</button>
          <button class="segmentBtn bg-gray-100 hover:bg-gray-200 px-4 py-2 rounded-md" data-segment="refi">Refinance</button>
        </div>
      </div>

      <!-- Step 3 ----------------------------------------------------------------- -->
      <div data-step="3" class="quiz-step hidden">
        <h2 class="text-xl font-semibold mb-4">What's your target timeline?</h2>
        <select id="timeline" class="w-full border rounded-md p-2 mb-6">
          <option>0‑3 months</option>
          <option>3‑6 months</option>
          <option>6‑12 months</option>
        </select>
        <button class="nextBtn bg-blue-600 hover:bg-blue-700 transition text-white px-4 py-2 rounded-md w-full">Continue</button>
      </div>

      <!-- Step 4 ----------------------------------------------------------------- -->
      <div data-step="4" class="quiz-step hidden">
        <h2 class="text-xl font-semibold mb-4">Biggest hurdle right now?</h2>
        <select id="hurdle" class="w-full border rounded-md p-2 mb-6">
          <option>Down Payment</option>
          <option>Credit Score</option>
          <option>High Rates</option>
          <option>Unsure</option>
        </select>
        <button id="finishBtn" class="bg-green-600 hover:bg-green-700 transition text-white px-4 py-2 rounded-md w-full">See My Game‑Plan</button>
      </div>
    </div>
  </div>

  <!-- ── DEEP‑DIVE SEGMENT PANELS ─────────────────────────────────────────── -->
  <section id="deepDive" class="py-16 px-4 hidden">

    <!-- First‑Timer Panel -->
    <div id="panel-firstTimer" class="panel hidden max-w-3xl mx-auto">
      <h2 class="text-2xl font-bold mb-4">First‑Time Buyer Game‑Plan</h2>
      <ul class="list-disc pl-6 mb-6 space-y-2">
        <li>Florida Bond + Hometown Heroes DPA guide</li>
        <li>3.5%‑down FHA cheat‑sheet</li>
        <li>$0 lender‑fee pre‑approval certificate</li>
      </ul>
      <form action="https://drmortgageusa.my1003app.com" target="_blank">
        <input type="hidden" name="segment" value="First Time Buyer"/>
        <button class="bg-blue-600 hover:bg-blue-700 transition text-white px-6 py-3 rounded-xl shadow-lg">Get My DPA Game‑Plan</button>
      </form>
    </div>

    <!-- Veteran Panel -->
    <div id="panel-veteran" class="panel hidden max-w-3xl mx-auto">
      <h2 class="text-2xl font-bold mb-4">Veteran Home‑Run Blueprint</h2>
      <ul class="list-disc pl-6 mb-6 space-y-2">
        <li>Top 3 VA loan myths—busted</li>
        <li>Funding‑fee waiver hack for disabled vets</li>
        <li>21‑day express closing roadmap</li>
      </ul>
      <form action="https://drmortgageusa.my1003app.com" target="_blank">
        <input type="hidden" name="segment" value="Veteran"/>
        <button class="bg-blue-600 hover:bg-blue-700 transition text-white px-6 py-3 rounded-xl shadow-lg">Lock In My VA Savings</button>
      </form>
    </div>

    <!-- Credit‑Boost Panel -->
    <div id="panel-creditBoost" class="panel hidden max-w-3xl mx-auto">
      <h2 class="text-2xl font-bold mb-4">Credit‑Boost Fast‑Track</h2>
      <ul class="list-disc pl-6 mb-6 space-y-2">
        <li>Top 5 score pitfalls stopping approvals</li>
        <li>Exclusive $100‑off Uqual credit‑repair offer</li>
        <li>60‑day rapid‑rescore checklist</li>
      </ul>
      <form action="https://drmortgageusa.my1003app.com" target="_blank">
        <input type="hidden" name="segment" value="Credit Boost"/>
        <button class="bg-blue-600 hover:bg-blue-700 transition text-white px-6 py-3 rounded-xl shadow-lg">Boost My Score</button>
      </form>
    </div>

    <!-- Refinance Panel -->
    <div id="panel-refi" class="panel hidden max-w-3xl mx-auto">
      <h2 class="text-2xl font-bold mb-4">Refi &amp; Save Strategy</h2>
      <ul class="list-disc pl-6 mb-6 space-y-2">
        <li>Break‑even calculator on today’s rates</li>
        <li>2‑1 buydown vs permanent buydown walkthrough</li>
        <li>Zero‑cost streamline refi checklist</li>
      </ul>
      <form action="https://drmortgageusa.my1003app.com" target="_blank">
        <input type="hidden" name="segment" value="Refinance"/>
        <button class="bg-blue-600 hover:bg-blue-700 transition text-white px-6 py-3 rounded-xl shadow-lg">Run My Savings Report</button>
      </form>
    </div>
  </section>

  <!-- ── SCRIPT ───────────────────────────────────────────────────────────── -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const quizModal = document.getElementById('quizModal');
      const steps = document.querySelectorAll('.quiz-step');
      let current = 0;
      let segment = '';

      const showStep = (i) => {
        steps.forEach((s, idx) => s.classList.toggle('hidden', idx !== i));
      };

      // Open quiz
      document.getElementById('startQuiz').addEventListener('click', () => {
        quizModal.classList.remove('hidden');
        current = 0;
        showStep(current);
      });

      // Close on outside click
      quizModal.addEventListener('click', (e) => {
        if (e.target === quizModal) quizModal.classList.add('hidden');
      });

      // Continue buttons
      document.querySelectorAll('.nextBtn').forEach(btn => {
        btn.addEventListener('click', () => {
          current++;
          showStep(current);
        });
      });

      // Segment selection
      document.querySelectorAll('.segmentBtn').forEach(btn => {
        btn.addEventListener('click', () => {
          segment = btn.dataset.segment;
          current++;
          showStep(current);
        });
      });

      // Finish
      document.getElementById('finishBtn').addEventListener('click', () => {
        quizModal.classList.add('hidden');

        const deepDive = document.getElementById('deepDive');
        deepDive.classList.remove('hidden');

        // Hide all panels then show the selected one
        document.querySelectorAll('#deepDive .panel').forEach(p => p.classList.add('hidden'));
        document.getElementById(`panel-${segment}`).classList.remove('hidden');

        // Smooth scroll to reveal
        deepDive.scrollIntoView({ behaviour: 'smooth' });
      });
    });
  </script>
</body>
</html>
